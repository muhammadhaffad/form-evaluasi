/** @type {import('tailwindcss').Config} */
export default {
    content: [
        // Tambahkan semua file PHP, HTML, atau JS tempat Anda menggunakan kelas Tailwind
        './*.php',         // Untuk file di root
    ],
    theme: {
        extend: {},
    },
    plugins: [],
}